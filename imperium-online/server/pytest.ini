[pytest]
# Make the `app` package importable from tests without an install step.
pythonpath = .
testpaths = tests
markers =
    real_clock: test drives app.daynight directly; don't pin the world clock to day
